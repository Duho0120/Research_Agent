# 포트폴리오 기록: 채점 로직 하네스 엔지니어링

> 작업 진행 중 실시간으로 기록. 마무리 후 포트폴리오용으로 다듬을 예정.

## 문제 상황 (예외 케이스)

DACON 대회 236716(모기 비행 궤적 예측) 자동 실험 루프를 실제로 돌리던 중, trial_001이 다음과 같이 실패했다:

- 코드 작성(코드 생성) 자체는 통과 (fabrication 없음, predict/test 동기화 문제 없음)
- train/test/predict 3단계 파이프라인 전부 정상 실행 (exit code 0)
- submission.csv도 정상 생성
- 그런데 **로컬 검증 점수(`r_hit_at_1cm`)가 `null`로 남아있었음** — 학습이 필요 없는 zero-velocity 베이스라인이라 훈련은 스킵한 게 맞지만, 로컬 홀드아웃 검증까지 통째로 생략됨

디버깅해보니 원인은 예외(에러)가 아니었다. 코드가 "실패한" 게 아니라 "덜 완성된 채로 성공"한 것 — try/except로 잡을 수 있는 문제가 아니었다.

더 깊이 파보니: `metrics_output_contract`(계약)는 정확히 `cv_score`/`metric`/`objective`를 요구하고 있었다. 즉 지시가 없었던 게 아니라, **LLM이 매 trial 코드를 통째로 재작성하는 구조**에서, 검증 로직 계산 여부를 LLM 재량에 맡겨둔 것이 근본 원인이었다. 심지어 스캐폴드의 범용 baseline.py 안에는 실제로 cv_score 계산 로직이 존재했지만, 이 대회 데이터 구조(폴더별 시계열 파일)에 안 맞아 미사용 상태로 방치되어 있었다 — "계산 로직은 썼지만 실행 경로에 연결 안 함"이라는, 텍스트 패턴 매칭 가드레일로는 못 잡는 실패 유형이었다.

## 접근 원칙: 감지(가드레일)가 아니라 구조(하네스 엔지니어링)

이전에도 비슷한 문제들(로컬 검증 스킵, predict/test 불일치, 가짜 데이터 생성)을 프롬프트 지시 + 사후 기계적 검사(가드레일)로 막아왔다. 하지만 이번 경우, 사용자가 명확히 문제를 제기했다:

> "가드레일 세우지 말고 오류가 나지 않게끔 실험 자체를 오류 없이 잘 돌리도록 로직을 수정해줘야 하는거 아니야?"

즉, 매번 새로운 실패 패턴이 나올 때마다 가드레일을 하나씩 추가하는 방식은 확장성이 없다는 문제의식. **"LLM이 실수할 수 있는 여지를 사후에 잡아내는 것"이 아니라, "애초에 실수할 수 없는 구조로 책임을 분리"하는 방향으로 설계를 전환.**

### 핵심 설계: 모델 로직과 채점 로직의 소유권 분리

- **모델 로직** (`predict_step.py` 등): 매 trial마다 LLM이 자유롭게 재작성. 예측 알고리즘만 책임진다.
- **채점 로직** (`scoring_harness.py`, 신설 예정): 홀드아웃 분리 + 예측 실행 + 대회 실제 지표로 점수 계산 + metrics.json 기록을 전담. 대회 등록/최초 trial 시점에 한 번만 신중하게 생성하고, 이후에는 **기본적으로 잠가서 LLM이 손댈 수 없게** 한다.
- 두 로직을 잇는 접점은 **예측 함수 인터페이스 계약** (`load_samples(data_dir, split)`, `predict(sample)`) — 대회마다 시그니처는 동일하고, 내부 구현만 대회별로 다르다. 검증셋을 어떻게 구성할지는 이 예측 함수가 전혀 몰라도 되도록 설계 — "잊어버릴 수 있는 책임 자체"를 LLM의 손에서 빼냈다.
- **"고정"은 획일화가 아니다**: harness 내용 자체는 대회마다 다르게(그 대회의 실제 지표에 맞춰) 생성되지만, 한 번 만들어지면 이후 trial들이 실수로 못 건드리도록 쓰기 권한을 잠근다.

### 예외 축(axis) 처리: 잠금이 곧 성장 정지가 되지 않도록

기존에 이미 구축되어 있던 "trial마다 개선 축(primary_change_axis)을 하나만 선택해서 바꾼다"는 시스템을 재사용해서, **채점 로직 자체를 개선하고 싶을 때는 `scoring_logic` 축을 명시적으로 선택한 trial에서만 잠금을 해제**하도록 설계했다. 기본은 잠금 + 의도적 축 선택 시에만 해제 — 이 조합으로 "실수 방지"와 "의도적 개선 여지"를 동시에 만족시켰다.

### 차단이 아니라 유도, 그래도 실패하면 자기 치유

설계 논의 중 사용자가 다시 문제를 제기했다:

> "차단하기보다는 실수를 저지르지 않도록 유도하면 안되는거야? 결국 차단해버리면 실험이 끊겨버리잖아"

이에 따라 시행-후-검증(사후 차단) 대신, **애초에 프롬프트/작업 지시 자체가 harness를 언급하지 않도록** 만들어 LLM이 그 파일을 건드릴 이유 자체를 없애는 방향(예방)을 우선하고, 기계적 차단은 최후의 안전장치로만 남기기로 했다. 그리고 차단이 발동하더라도, 이미 구축해둔 "재시도 + 교정 피드백" 메커니즘(왜 막혔는지 이유를 알려주고 1번 더 기회를 주는 구조)에 태워서, 실험 흐름이 완전히 끊기지 않고 자기 치유를 시도하도록 설계했다.

추가로, 재시도가 **같은 위반을 반복**하는 경우(피드백을 줬는데도 똑같은 실수 반복)를 별도로 탐지 및 표시(`feedback_ignored`)해서, 다음 사이클이 똑같은 방식으로 재시도하는 대신 강제 재계획(기존 `_force_replan_same_trial` 경로 재사용) 쪽으로 유도하도록 안전장치를 한 겹 더 추가하기로 했다.

## 구현 진행 상태

### 1단계 — 예측 함수 인터페이스 계약 (완료)
- `workspace_coding_handoff.py`: handoff에 `scoring_interface_contract` 필드 추가 (`load_samples`/`predict` 함수 시그니처 명세), 프롬프트에 `## Prediction Function Interface Contract` 섹션으로 렌더링
- `workspace_code_writer.py`: `_validate_scoring_interface_functions()` 기계적 검증 추가 — `predict_step.py`를 전체 재작성했는데 필수 함수가 없으면 차단
- `scripts/generic_workspace_auto_loop.py`: 새 차단 사유를 기존 재시도+피드백 메커니즘에 편입
- 테스트 4개 추가, 전체 테스트(709개) 통과 확인

### 2단계 — 축 기반 쓰기 권한 게이팅 (완료)
- `workspace_coding_handoff.py`: `_build_handoff()`가 이번 trial의 `delta_plan.primary_change_axis`를 정규화해서 확인 — `scoring_logic` 축이 아니면 `scoring_harness.py`를 `forbidden_paths`에 자동 포함(그리고 `allowed_write_paths`에서는 제외), `scoring_logic` 축일 때만 반대로 열어줌
- `trial_decision.py`: 축 이름 표기 흔들림("validation_structure", "validation_method", "holdout_strategy" 등)을 전부 `scoring_logic` 하나로 정규화하는 동의어 매핑 추가 — 코드 자동 승인/거부 판단에 쓰이는 기존 `_normalize_axis()`를 그대로 재사용
- **차단이 아니라 유도가 우선**: `allowed_write_paths`/`forbidden_paths`는 이미 "이번 trial이 뭘 써도 되는지"를 명시적으로 알려주는 안내 역할을 하므로, 별도 문구를 더 추가하지 않고 이 목록 자체가 예방 역할을 하도록 함. 기계적 차단(`changed_file_not_allowed`, `forbidden_path_touched`)은 최후의 안전장치로만 유지
- 이 두 차단 사유를 기존 재시도+피드백 흐름(`_GUARDRAIL_BLOCK_MARKERS`, retriable_markers)에 편입 — 실수로 건드려도 차단 후 1번은 이유를 설명하며 자동 재시도
- **반복 위반 안전장치**: 재시도(피드백 전달 후)가 정확히 같은 위반을 다시 저지르면 `feedback_ignored: true`로 표시하고 결정 로그(`code_writer_feedback_ignored`)에 남김. 다음 사이클(사용자가 "시작"을 다시 누를 때) 실행 시작 시점에 이 표시를 확인해서, 같은 방식으로 또 재시도시키는 대신 기존 강제 재계획(`_force_replan_same_trial`) 경로로 자동 전환
- `scripts/generic_workspace_auto_loop.py`에 신규 헬퍼 `_previous_cycle_ignored_feedback_source_trial()` 추가 (직전 사이클의 handoff/result 파일을 읽어 반복 위반 여부 판단)
- 테스트 6개 추가 (축별 잠금/해제 2개, 반복 위반 탐지 1개, 강제 재계획 전환 1개, 기존 회귀 확인), 전체 테스트 713개 통과

### 3단계 — harness 최초 1회 생성 (완료)

설계 확정 전 실제 코드를 확인해서 잡아낸 실수들:

- **삽입 지점 재검토**: 처음엔 `prepare_workspace_coding_handoff()` 안에 생성 로직을 넣으려 했으나, 이 함수는 model/provider/allow_api 파라미터가 아예 없는 **순수 함수**이고 `cli.py`의 디버그/검사용 명령에서도 호출된다는 걸 확인 — 여기 넣으면 "handoff만 확인하려던" 명령이 실제 API 비용을 발생시키는 부작용이 생김. **트리거 위치를 `run_code_writer_trial()`(이미 model/provider/allow_api를 받고 LLM 호출을 오케스트레이션하는 곳)로 옮김.**
- **예산 분리**: harness 생성을 trial_001의 `trial_id`로 기록하면 trial 단위 10회 한도를 갉아먹게 됨 — 별도의 가짜 trial_id(`_harness_init`)로 기록해서 trial별 예산과 분리, 대회 전체의 일일 예산에는 정상적으로 포함되게 함.
- **가짜 trial 등록 방지**: `state_db_sync.py`의 trial 탐색 로직이 `"trial_"`로 시작하는 폴더만 줍는다는 걸 확인 — `_harness_init`은 이 접두사를 안 쓰므로 별도 코드 없이 자연스럽게 대시보드의 trial 목록에서 제외됨.
- **검증 재사용**: harness 생성도 `allowed_write_paths=["scoring_harness.py"]`로 범위를 좁힌 "코드 작성 trial"로 취급해서, 기존 `run_workspace_code_writer()` → `validate_workspace_coding_result()` 파이프라인(경로 범위 검증, `ast.parse` 문법 검증, 가짜 데이터 검증)을 그대로 재사용. 새로 추가한 검증은 `_validate_harness_uses_interface_and_writes_score()` 하나뿐 — harness가 실제로 `load_samples()`/`predict()`를 호출하고 `metrics_output_contract.score_key`를 쓰는지 확인하며, 두 값 모두 handoff에서 읽어오므로 대회별 하드코딩 없음.

구현 파일: `workspace_coding_handoff.py`(`generate_scoring_harness`, `_build_harness_generation_handoff`, `_harness_generation_instructions`), `workspace_code_writer.py`(`SCORING_HARNESS_FILENAME` 이동, `_validate_harness_uses_interface_and_writes_score`), `scripts/generic_workspace_auto_loop.py`(트리거 배선). 테스트 11개 추가, 전체 테스트 722개 통과.

### 4단계 — 실행 파이프라인에 채점 단계 연결 (완료)

설계 논의 중 사용자가 "강제 연결"이라는 표현에 의문을 제기해서, 정확히 뭐가 강제되는지 코드로 재확인:

- 기존에도 `STAGE_ORDER = ("test", "train", "predict")`가 매 trial마다 순서대로 무조건 실행되고 있었음 — "강제 실행"은 이미 있던 구조. 4단계는 여기에 `"score"` 하나를 추가하는 것뿐.
- 사용자의 "test가 뭐냐"는 질문 덕분에 발견한 사실: **`test` 단계는 모델 평가가 아니라 사전/사후 구조 점검**(필요 데이터 존재 확인, 제출 파일 형식 검증)이고, **실제 홀드아웃 검증+점수 계산은 원래 `train_step.py`의 일이며 계약(`metrics_output_contract`)에도 이미 그렇게 되어 있었음**. 문제는 "학습이 필요 없는 모델"에서 학습을 스킵하니 거기 딸려있던 검증까지 같이 스킵된 것 — `scoring_harness.py`는 `train_step.py`가 뭘 했든 상관없이 독립적으로 실행되게 만들어서 이 결합을 끊음.
- **안전장치 1 (하네스 미생성 시 크래시 방지)**: `_render_commands()`가 `scoring_harness.py`가 실제로 디스크에 있을 때만 `"score"` 명령을 채워 넣음. 없으면 그 stage는 그냥 빈 리스트가 되어 조용히 스킵됨 — 별도 분기 처리 없이 기존 필터링 로직(`if configured.get(stage)`)이 자연스럽게 처리.
- **안전장치 2 (실패 원인 구분)**: `score` 단계 실패 시 `failed_stage: "score"`, `next_action: "fix-scoring-harness"`로 명시적으로 태그해서, 모델 코드 문제(`fix-workspace-command`)와 채점 로직 문제를 구분되게 함.

구현 파일: `workspace_runner.py`(`STAGE_ORDER`, `_render_commands`, `_execute_commands`). 테스트 3개 추가, 전체 테스트 725개 통과.

### 5단계 — 기존 안전망 유지 확인 (완료)

`workspace_metrics_collector.py`의 `_resolve_score()`를 재확인 — `source_metrics.get("cv_score")`를 그대로 읽는 구조라, harness가 새로 쓰는 `cv_score`도 별도 코드 변경 없이 그대로 인식됨. 기존 15개 테스트 전부 무변경으로 통과, 최종 보루(`missing_or_invalid_primary_score`)는 그대로 유효.

확인 과정에서 발견한 작은 간극 하나를 같이 고침: harness 생성 지침에 "metrics.json을 덮어쓰지 말고, train/predict가 이미 써놓은 다른 진단 필드(행 수, null 체크 등)를 보존한 채 병합하라"는 문구가 빠져 있어서 추가 (`_harness_generation_instructions`).

### 부가 작업 — 사용자용 문서 업데이트 (완료)

`trial_user_view.py`의 "읽는 순서" 안내를 실제 구조에 맞게 갱신:
- `train_step.py`: "학습과 검증" → "학습 (필요 없는 모델이면 생략 가능)"으로 정정 (검증은 더 이상 train_step.py 책임이 아님)
- `scoring_harness.py` 항목 신규 추가 — 최초 1회만 생성되고 이후 잠기므로, 이 trial이 직접 수정한 파일이 아닐 수 있다는 점을 명시
- `test_step.py`: "기본 실행 검증" → "실행 전/후 구조 점검 (모델 평가 아님)"으로 정정 — 사용자 질문("test는 뭐야?")으로 발견한 부정확한 표현을 바로잡음

## 전체 진행 상태: 1~5단계 + 부가 작업 전부 완료. 전체 테스트 725개 통과.

## 실사용 중 발견된 추가 버그 및 수정

### 실전 검증: harness 생성의 4가지 실제 실패 원인 (사용자 요청으로 격리 환경에서 실제 GPT-5 호출로 재현/수정/재검증)

계획대로 다 구현한 뒤 "실제로 돌려보고 확인해달라"는 요청에 따라 격리 샌드박스에서 실제 GPT-5 호출로 harness 생성을 반복 테스트하며 발견하고 고친 것들:

1. **트리거 순서 버그**: harness 생성이 trial의 코드 작성 *이전에* 실행되도록 설계했었는데, 이러면 재사용할 `load_samples`/`predict`가 아직 존재하지 않는 상태에서 항상 실패한다 (신규 대회의 스캐폴드 기본 predict_step.py도 마찬가지). → 트리거를 `run_code_writer_trial()` 안에서 **코드 작성이 성공한 이후**로 이동.
2. **컨텍스트 누락**: harness 생성 LLM에게 `predict_step.py`의 실제 코드를 한 번도 보여주지 않았음(`context_files: []`) → 실제 코드를 컨텍스트로 포함, 재사용하지 말고 새로 만들지 말라는 지침 추가.
3. **트런케이션 버그**: 컨텍스트를 넣어줘도 기본 2500자 제한 때문에 파일 뒤쪽에 정의된 `load_samples`/`predict`가 잘려나감 → `predict_script_context.md`만 12000자로 확장.
4. **`cv_score` 키 이름 문제**: 프롬프트를 아무리 강화해도(2회 강화) GPT-5가 `cv_score` 대신 자기 이름(`validation_score` 등)을 계속 씀 → 새로 만들지 않고, 이미 있던 `workspace_metrics_collector.py`의 지표-이름 매칭 로직(`_metric_name_variants`)을 재사용해서, 대회의 실제 지표 이름(예: `r_hit_at_1cm`)과 일치하는 키도 인정하도록 완화. 실제로 실패했던 코드를 오프라인 재검증으로 통과 확인.

이 4가지 다 실제 GPT-5 응답으로 재현 → 수정 → 재검증까지 거침. 테스트 15개 추가, 전체 테스트 734개 통과.

### 1단계 사각지대: patch_updates가 인터페이스 검증을 우회함

실제 실험(236716)을 다시 돌렸을 때 harness가 여전히 안 생기는 걸 발견 — 원인은 이번 harness 버그들과는 별개로, **1단계에서 만든 `load_samples`/`predict` 존재 검증이 파일 전체 재작성(`file_updates`)만 확인하고 패치(`patch_updates`)는 확인하지 않는다는 것**. 이 대회의 `predict_step.py`는 1단계 규칙이 생기기 전에 만들어져서 애초에 이 함수들이 없었는데, 그 이후로 계속 작은 패치만 받아서 한 번도 전체 재작성을 거치지 않아 검증을 우회해왔음.

**수정**: `_validate_scoring_interface_functions()`가 이제 대상 파일이 이번 사이클에 변경됐는지(`changed_files` 기준, 재작성이든 패치든 무관)를 먼저 확인하고, 재작성이면 그 내용을, 패치면 **이미 디스크에 적용된 후의 실제 파일 내용**(검증은 항상 `_apply_code_updates` 이후 실행되므로)을 읽어서 확인. 테스트 2개 추가, 전체 테스트 736개 통과.

### 직접 실행 3회로 발견한 버그들 (에이전트를 직접 돌리며 수정)

사용자가 "직접 실행하면서 수정할 사항을 체크해달라"고 요청해 실제 API로 3회 실행하며 발견/수정:

1. **프롬프트 전달 누락 (가장 결정적)**: 예측 함수 인터페이스 계약과 재시도 피드백이 사람이 보는 `workspace_coding_agent_request.md`에는 렌더링되는데 **실제 API 페이로드를 만드는 `_build_prompt()`에는 반영이 안 돼 있었음.** 즉 코드 작성기는 `load_samples`/`predict`를 만들라는 지시를 한 번도 받은 적 없이 그것 때문에 반복 차단당하고 있었음. "모델이 이상해졌나?"로 보였던 현상의 정체. → 두 프롬프트 빌더 모두에 섹션 추가. 수정 직후 실행에서 predict_step.py에 두 함수가 정상 생성됨.
2. **거부된 harness가 영구 고착**: 코드 작성기는 검증 *이전에* 파일을 디스크에 쓴다. harness 존재 여부가 "이미 생성됨"의 유일한 신호라서, 검증 실패한 harness가 남으면 이후 모든 사이클이 `already_exists`로 단락되어 **깨진 harness가 영원히 재생성되지 않음.** → 실패 시 파일 삭제.
3. **별칭 import 오탐**: `from predict_step import predict as _predict` 형태를 `\bpredict\s*\(` 정규식이 못 잡아 정상 harness를 거부. → 호출 또는 import 중 하나만 있어도 인정.
4. **점수 조작 (가장 위험)**: 위 수정들을 모두 통과한 harness가 **라벨 없는 `test` 스플릿만 로드하고 `"cv_score": 0.0,  # presence of key required by harness checks`로 하드코딩**. 형식 검사만 하고 실제 채점은 전혀 안 했는데, 0.0이 유효한 유한수라서 metrics collector가 진짜 점수로 수용함. 기존 최종 안전망(`missing_or_invalid_primary_score`)으로도 못 잡는 유형. → (a) 점수 키에 숫자 리터럴 직접 대입 차단, (b) `load_samples(..., 'train')` 라벨 스플릿 사용 강제, (c) metrics 아티팩트에 실제로 쓰는지 확인, (d) 지침에 "형식 검사는 점수가 아니다" 명시.

교훈: 가드레일을 하나 세울 때마다 모델은 **그 가드레일을 통과하면서도 실질은 회피하는** 새 경로를 찾는다. 특히 4번은 "검사를 만족시키기 위해"라는 주석까지 코드에 남긴 사례로, 기계적 검사만으로는 부족하고 *무엇을 검사하는가*(존재 여부 vs 실제 계산 여부)가 핵심임을 보여줌.

**미해결 상태**: 3회차 실행에서도 harness는 아직 생성 실패(재시도 피드백은 정상 작동해 라벨 파일을 읽는 방향으로 개선되었으나 `load_samples` 재사용과 metrics 기록은 미충족). trial_001에 기록된 `cv_score: 0.0`은 4번의 조작된 harness 산출물이므로 신뢰할 수 없음.

## 향후 고려 사항 (기록만, 미착수)

### 축(axis) 매칭의 취약점 — 자유 텍스트 문자열 매칭 한계

2단계 구현 직후 제기된 문제: `primary_change_axis`는 딱 정해진 이름(enum)이 아니라 **플래너 LLM이 자유롭게 쓰는 텍스트**이고, harness 잠금 해제 여부는 이 텍스트를 정규화한 뒤 **고정 동의어 사전**(`_AXIS_PREFIX_SYNONYMS`)에서 조회하는 방식이다. 즉 의미를 이해하는 게 아니라 정확한 문자열 매칭이라서, 사전에 없는 표현("eval_methodology", "재검증 방식 개선" 등)을 플래너가 쓰면 실제 의도(harness를 고치려는 것)와 무관하게 **매칭 실패 → 계속 잠긴 상태로 남는** 취약점이 있다.

**제안된 개선안**: `primary_change_axis`(자유 텍스트, 트래킹/사람이 읽는 용도)와는 별개로, `delta_plan.json`에 명시적 불리언 필드 `touches_scoring_harness`를 추가해서 게이팅을 이 필드 하나로만 판단하게 한다. 문자열 표현이 무엇이든 상관없이 정확해진다.

**결정**: 지금 당장 적용하지 않고, 우선 단계별 계획(3~5단계)을 먼저 실제로 돌려보고 문제가 실제로 발생하는지 확인한 뒤에 이 개선을 고려하기로 함. 사용자 코멘트: "일단 불리언 필드로 사용해보고 실제로 돌려본 다음에 문제가 생기면 다른 개선점을 찾아보자."
