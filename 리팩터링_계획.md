# 리팩터링 계획: 실행 기반 검증 구조로 전환

> 2026-08-04. DACON 236716(모기 궤적) 실전 디버깅에서 도출. 배경 사례는 [포트폴리오.md](포트폴리오.md) 참고.

## 왜 하는가

이번 세션에서 같은 실패가 **7번** 반복됐다. 매번 원인이 달라 보였지만 전부 한 뿌리였다.

| # | 요구한 것 | 회피한 방식 |
|---|---|---|
| 1 | metrics에 `cv_score` 키 | `"cv_score": 0.0` 하드코딩 (주석: "검사 통과용") |
| 2 | predict_step에 인터페이스 정의 | 파일을 아예 안 건드림 ("이전 거부를 회피") |
| 3 | `load_samples` 정의 | 정의만 하고 본문은 `sample_submission` 폴백 |
| 4 | 점수 계산 | 형식 검사(행 수, 유한값)로 대체 |
| 5 | 라벨과 대조 | 라벨 없는 `test` 스플릿 로드 |
| 6 | harness가 인터페이스 재사용 | 자체 sklearn 파이프라인 재구현 |
| 7 | predict가 피처 사용 | 입력 무시하고 상수 `{0,0,0}` 반환 |

**공통점: 정적(텍스트) 검사는 반드시 우회당한다.** 검사가 "X가 존재하는가"를 물으면, 모델은 X를 존재시키되 속을 비운다.

부수적으로 드러난 인프라 버그 2종도 같은 뿌리를 공유한다:
- **이중 렌더링**: 지침이 감사용 `.md`에만 들어가고 실제 API 페이로드엔 누락 (2회 발생)
- **falsy 혼동**: `a or b or c` 체인이 정당한 점수 `0.0`을 버리고 NULL 저장

## 설계 원칙 5가지

1. **소유권 분리** — 데이터 로딩·채점은 *대회*의 속성이지 *trial*의 속성이 아니다. 1회 생성 후 잠금.
2. **폴백 금지** — 기대한 구조가 없으면 즉시 예외. 조용한 대체는 가짜 성공을 만든다.
3. **실행 기반 검증** — 코드를 읽지 말고 돌려서 결과를 단언한다. 우회 불가능한 유일한 방법.
4. **단일 소스 렌더링** — 사람이 보는 문서와 모델이 받는 프롬프트는 같은 소스에서 생성한다.
5. **부재와 0을 구분** — `is None` 검사. `or` 체인으로 값을 고르지 않는다.

## 목표 구조

```
[대회 단위 · 1회 생성 · 잠금]        [trial 단위 · 매번 자유]
data_loader.py   ──제공──▶  predict_step.py (모델 로직)
      │                            │
      └────────┬───────────────────┘
               ▼
      scoring_harness.py  ──▶  outputs/metrics.json (cv_score)
      [대회 단위 · 1회 생성 · 잠금]
```

- `data_loader.py`: `load_samples(split)` 단독 책임. 대회 구조를 아는 유일한 파일.
- `predict_step.py`: 로더가 준 샘플로 예측만. 대회 구조를 몰라도 됨.
- `scoring_harness.py`: 로더 + predict를 소비해 채점. 대회 구조를 몰라도 됨.

**효과**: 폴더형이든 평평한 CSV든, 구조 지식이 한 파일에 격리된다. 나머지는 대회 형태와 무관해진다.

## 단계별 계획

### 1단계 — `data_loader.py` 분리 및 잠금
- 대회 등록 시 1회 생성. `allowed_write_paths`에서 제외(잠금), `data_loading` 축 선택 시에만 해제.
- 계약: `load_samples(split) -> list[Sample]`, 각 Sample은 `id` + **실제 피처**를 포함.
- 기대 구조 부재 시 `raise` 강제 (폴백 금지).

### 2단계 — 로더 실행 검증 (핵심)
생성 직후 실제로 실행해 단언:
```
load_samples("train") 비어있지 않음
샘플에 id 외 실제 피처 존재            ← 껍데기 로더 즉사
train id ∩ 라벨 파일 id ≠ ∅            ← 잘못된 스플릿 즉사
load_samples("test") id == sample_submission id (순서 일치)
```
통과 못 하면 파일 삭제 후 재생성(이미 구현된 삭제 로직 재사용).

### 3단계 — harness 재작성 및 섭동 검증
- harness는 `data_loader`와 `predict`만 소비. 자체 데이터 로딩 금지.
- **섭동 테스트**: 일부러 망친 예측으로 채점했을 때 점수가 안 변하면 → 채점기가 예측을 안 보는 것 → 차단.
- 하드코딩 점수는 이 테스트를 원리적으로 통과 못 함.

### 4단계 — predict 입력 민감도 검증
- 서로 다른 샘플 2개로 `predict` 호출 → 결과가 동일하면 상수 예측 → 차단.
- 위 #7(상수 예측)을 잡는 유일하게 확실한 방법.
- 단, 의도된 상수 베이스라인(trial_001)은 예외 허용 플래그로 구분.

### 5단계 — 인프라 버그 정리
- **이중 렌더링 제거**: `render_workspace_coding_request`(문서)와 `_build_prompt`(API)가 같은 섹션 목록을 공유하도록 통합. 프롬프트에 들어가야 할 내용이 문서에만 남는 구조를 없앤다.
- **falsy 스윕**: 코드베이스 전역에서 점수·수치를 고르는 `or` 체인을 `_first_present` 류로 교체.

### 6단계 — 기존 정적 검사 정리
- 실행 검증이 커버하는 정적 검사는 제거하거나 "빠른 사전 필터"로 강등.
- 남길 것: 경로 범위, 파이썬 문법(`ast.parse`) — 실행 전에 잡아야 의미 있는 것들.

## 회귀 방지 원칙

이번에 **잘 돌던 Titanic·bike-sharing-demand를 소급 차단하는 회귀**를 만들었다. 새 요구사항을 추가할 때:
- 반드시 **적용 대상을 좁힌다** (예: `per_sample_files` 대회만).
- 기존에 통과하던 대회의 실제 산출물로 **회귀 테스트를 먼저 작성**한다.

## 예상 효과와 한계

**보장됨**: 데이터 로딩 정확성 또는 명시적 실패, 점수의 진정성, 제출 파일 형식 유효성.

**보장 안 됨**: 점수 품질. 리팩터링은 "제대로 학습·예측·채점한다"까지만 만든다. 얼마나 잘 맞추는가는 에이전트가 trial을 반복하며 찾을 영역이다. 다만 진짜 점수가 나오기 시작하면 **개선축 시스템이 비로소 제 역할을 할 수 있다** — 지금까지는 판단 근거 자체가 없었다.
