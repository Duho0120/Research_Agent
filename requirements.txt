langgraph>=1.0.5
langgraph-checkpoint-sqlite==3.0.2
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
kaggle>=1.6
requests>=2.28
beautifulsoup4>=4.12
# DACON has no PyPI package; DACON distributes this wheel from their official
# forum post (https://dacon.io/en/forum/403557) via a Google Drive link, not
# an index, so it is vendored here for reproducible installs:
./vendor/dacon_submit_api-0.0.4-py3-none-any.whl
