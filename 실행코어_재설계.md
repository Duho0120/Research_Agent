# 실행 코어 재설계 (execution_core)

> 목적: 실험 실행 모델을 "네 개의 독립 스크립트"에서 "프레임워크가 소유하는 하나의 계약"으로 교체한다.
> 범위: 실행 모델 약 6,000줄만. 실험 규칙 · LangGraph · RAG · UI · DACON 연동은 건드리지 않는다.

---

## 1. 왜 다시 짓는가

### 근본 결함 한 문장

**학습과 추론의 연결이 코드 계약이 아니라 관례로만 존재한다.**

현재 파이프라인은 `execution_profile.yaml`이 지시하는 대로 네 개의 독립 프로그램을 순서대로 subprocess로 실행한다:

```
python test_step.py  →  python train_step.py  →  python predict_step.py  →  python scoring_harness.py
```

프레임워크가 각 단계에 대해 아는 것은 **returncode 뿐**이다. `train_step.py`가 모델을 저장하지 않아도, `predict_step.py`가 그것을 읽지 않아도, 둘 다 정상 종료한다. 단계 사이를 잇는 것은 파일시스템 경로에 대한 **암묵적 합의**이고, 그 합의를 강제하는 코드는 없다.

### 그래서 생긴 것

실패 사례가 나올 때마다 사후 검사를 하나씩 추가해왔다:

| 실제 사건 | 추가한 검사 | 진짜 원인 |
|---|---|---|
| 점수가 코드에 리터럴로 박혀 있었다 | 리터럴 점수 거부 | 에이전트가 채점기를 소유 |
| 이전 trial의 점수가 이번 결과로 보고됐다 | 아티팩트 mtime 신선도 검사 | 단계 간 통신이 파일 |
| 채점기가 예측을 무시하고 상수를 냈다 | 섭동 테스트 | 에이전트가 채점기를 소유 |
| 검증셋에 라벨이 없었다 | 분할 텍스트 검사 | 에이전트가 분할을 소유 |
| 선언한 것과 다른 파일명에 결과를 썼다 | 아티팩트 선언 대조 | 단계 간 통신이 파일 |
| predict가 모든 샘플에 같은 값을 냈다 | 예측 민감도 프로브 | **(계약과 무관 — 환원 불가능)** |

**6개 중 5개가 계약 부재를 사후 관찰로 때운 것이다.** 이 방식은 끝이 없다. 새 실패 사례마다 검사가 하나씩 늘고, 검사가 늘수록 우회 경로도 늘어난다 — 실제로 상수 예측 검사는 `run_code_writer_trial` 안에 있어서 resume 경로가 통째로 우회했다.

### 재설계의 약속과 그 한계

**약속**: 오류의 *종류*를 구조적으로 제거한다. 위 표의 5개는 검사가 아니라 **함수 시그니처와 소유권**으로 불가능해진다.

**한계 (정직하게)**: "오류가 날 여지가 없는" 설계는 없다. 상수 예측처럼 *어떤 계약에서도 문법적으로 합법이지만 무의미한* 코드는 실행 관찰로만 잡힌다. 재설계가 주는 것은 그런 관찰이 **한 곳에만 있고, 우회 경로가 존재하지 않는다**는 보장이다.

---

## 2. 설계 원칙

1. **프레임워크가 실행 흐름을 소유한다.** 에이전트는 프로그램을 쓰지 않고 함수를 채운다.
2. **점수는 에이전트가 만질 수 없다.** 분할도, 메트릭 계산도, 기록도 프레임워크가 한다.
3. **단계 간 통신은 메모리다.** 파일을 경유하지 않으면 낡은 파일 문제가 존재할 수 없다.
4. **실행 경로는 하나다.** resume이든 신규든 재시도든 같은 함수를 지난다.
5. **구조를 가정하지 않는다.** 대회마다 파일 구조가 다르므로, 데이터 접근은 에이전트가 정하고 프레임워크는 두 개의 앵커(제출 템플릿 · 라벨 원본)로만 검증한다.
6. **조용한 대체 금지.** 기대한 구조가 없으면 크게 실패한다.

---

## 3. 계약 인터페이스

### 3-1. 대회 단위 자산 (한 번 생성 후 잠금)

```python
# data_loader.py  — 대회마다 1회 생성, 이후 잠금
def load_samples(data_dir: str, split: str) -> list[dict]:
    """split은 "train" 또는 "test".
    각 원소는 최소한 {"id": ..., ...features...} 를 가진다.
    train split은 추가로 라벨 키를 가진다.
    구조 탐색은 이 함수 안에서 끝난다 — 다른 어떤 파일도 data/ 를 읽지 않는다."""

def label_keys() -> list[str]:
    """train 샘플에서 어떤 키가 정답인지 선언한다. 프레임워크가 분할 시 이걸 쓴다."""

def submission_columns() -> list[str]:
    """제출 파일의 컬럼 순서. 제출 템플릿에서 읽어 선언한다."""
```

> **이것이 유일하게 대회 구조를 아는 곳이다.** DACON은 대회마다 구조가 다르므로 고정 전략을 주지 않는다. 에이전트가 실제 파일을 보고 판단해 작성하고, 프레임워크는 결과를 앵커로 검증한다.

### 3-2. trial 단위 코드 (매 trial 재작성)

```python
# model.py  — trial마다 에이전트가 새로 쓰는 유일한 파일
def fit(train_samples: list[dict]) -> object:
    """학습하고 학습 결과를 반환한다.
    학습이 필요 없는 규칙 기반 모델이면 None 또는 파라미터 딕셔너리를 반환해도 된다."""

def predict(fitted: object, sample: dict) -> dict:
    """fit이 반환한 것을 인자로 받아 한 샘플을 예측한다.
    반환은 submission_columns()의 id 외 컬럼을 키로 갖는 딕셔너리."""
```

**핵심은 `predict`가 `fitted`를 인자로 받는다는 것이다.** 학습→추론 연결이 검사 항목이 아니라 함수 시그니처가 된다. `fit`의 산출물을 쓰지 않고 예측하는 것은 여전히 가능하지만(상수 예측), 그건 원래 관찰로만 잡히는 부류다.

### 3-3. 프레임워크가 소유하는 것 (에이전트 쓰기 금지)

```python
# execution_core/metrics.py — 대회 메트릭 구현
# 에이전트가 아니라 온보딩 단계에서 확정되고, trial 코드는 접근하지 못한다.
def compute(metric_name: str, predictions: list[dict], truths: list[dict]) -> float
```

메트릭 구현 자체는 대회마다 다르므로 여전히 생성이 필요하지만, **생성 시점이 trial 루프 밖(대회 온보딩)이고 trial 코드의 쓰기 범위에 들어가지 않는다.** 개선축이 `scoring_logic`으로 지정된 경우에만 잠금이 풀린다 — 기존 축 게이팅 규칙 그대로.

---

## 4. 실행 흐름

프레임워크가 이 순서를 직접 실행한다. 에이전트가 개입할 수 있는 지점은 `fit`과 `predict` 뿐이다.

```
execution_core.run_trial(competition, trial_id)
│
├─ 1. samples = loader.load_samples(data_dir, "train")     ← 잠긴 자산
├─ 2. train, val = framework_split(samples, label_keys())  ← 프레임워크 소유 (라벨 보장)
├─ 3. fitted = model.fit(train)                            ← 에이전트 코드
├─ 4. val_preds = [model.predict(fitted, s) for s in val]  ← 프레임워크가 fitted를 넘김
│      └─ 여기서 예측 민감도 관찰 (distinct 개수)          ← 환원 불가능한 검사 ①
├─ 5. score = metrics.compute(metric, val_preds, val_labels) ← 프레임워크 소유
├─ 6. test = loader.load_samples(data_dir, "test")
│      └─ 앵커 검증: test id ↔ 제출 템플릿 id             ← 환원 불가능한 검사 ②
├─ 7. sub_preds = [model.predict(fitted, s) for s in test]
├─ 8. 프레임워크가 outputs/submission.csv 작성
└─ 9. 프레임워크가 outputs/metrics.json 작성 (score 포함)
```

### 격리

`fit`/`predict`는 여전히 별도 프로세스에서 돌린다 — 에이전트 코드가 프레임워크 상태를 오염시키거나 무한 루프로 전체를 멈추는 것을 막기 위해서다. 다만 **프로세스 경계는 프레임워크가 통제하고, 경계를 넘는 데이터 형식도 프레임워크가 정의한다.** 에이전트가 파일 경로를 고르는 일은 없다.

무거운 학습(딥러닝 등)은 `fitted` 객체를 메모리로 들고 다닐 수 없다. 이 경우 **프레임워크가** 지정한 경로에 직렬화하고 복원한다. 에이전트에게는 여전히 `fit` → `predict(fitted, ...)` 로 보인다.

---

## 5. 무엇이 사라지고 무엇이 남는가

### 구조적으로 불가능해지는 것 (검사 삭제)

| 기존 실패 | 왜 불가능해지는가 |
|---|---|
| 학습 결과가 추론에 안 이어짐 | `predict(fitted, ...)` — 프레임워크가 직접 넘김 |
| 낡은 아티팩트를 이번 결과로 보고 | 단계 간 파일 통신 없음 |
| 점수 하드코딩 | 프레임워크가 계산 |
| 채점기가 예측을 무시 | 에이전트가 채점기를 못 씀 |
| 검증셋에 라벨 없음 | 프레임워크가 분할 |
| 다른 파일명에 결과 씀 | 프레임워크가 씀 |
| 제출 파일 컬럼 순서 오류 | 프레임워크가 `submission_columns()`로 씀 |
| resume 경로가 검사 우회 | 실행 경로가 하나 |

### 남는 것 (환원 불가능 — 실행 관찰로만)

| 검사 | 이유 | 재사용 |
|---|---|---|
| ① 예측 민감도 (상수 예측) | 어떤 계약에서도 문법상 합법 | 기존 `run_predict_sensitivity_probe` |
| ② 로더 앵커 대조 (id ↔ 제출 템플릿, 라벨) | 구조를 가정하지 않으므로 관찰만 가능 | 기존 `evaluate_loader_contract` |
| ③ 메트릭 구현 검증 (섭동) | 메트릭이 대회마다 다름 | 기존 `run_scoring_perturbation_probe` |

**기존 `runtime_contract.py` 400줄은 그대로 살린다.** 이 세 개는 어떤 설계에서도 필요하고, 이미 실제 데이터로 검증됐다.

---

## 6. 기존 시스템과의 경계 (건드리지 않는 것)

### 이어붙일 지점은 딱 하나

LangGraph 루프는 `WorkspaceLoopCallbacks.run_trial` 콜백을 통해 실행 단계를 호출한다
(`graph/workspace_loop_graph.py:43`). **새 코어는 이 콜백 뒤로 들어간다.** 그래프 노드 구조, 상태 정의, 체크포인터는 변경 없음.

### 하위 호환의 핵심

새 코어는 **기존과 동일한 경로에 동일한 형식의 아티팩트를 쓴다**:

- `outputs/metrics.json` — `collect_workspace_metrics`가 그대로 읽는다
- `outputs/submission.csv` — 제출 경로가 그대로 동작한다
- `workspace_run.json` — 상태 DB 동기화가 그대로 동작한다

따라서 **아래는 한 줄도 바뀌지 않는다**:

| 영역 | 파일 | 이유 |
|---|---|---|
| 실험 규칙 | `trial_decision.py` (3연속 미개선→축 변경, 후보 중복 거부) | metrics + plan만 소비 |
| 인사이트 반영 | `user_insight_policy.py` | 계획 단계에서 동작 |
| LangGraph | `graph/` 전체 | 콜백 뒤에서만 교체 |
| RAG | `retrieval/` 전체 | 계획 단계에서 동작 |
| 대시보드 · CLI | `web_app.py`, `cli_app.py`, `local_app.py`, `trial_user_view.py` | 아티팩트 형식 동일 |
| 상태 DB | `state_db*.py` | 아티팩트 형식 동일 |
| DACON 연동 | `integrations/dacon_api.py`, `agents/submission.py` | 제출 파일 경로 동일 |

### 결국 퇴역하는 것 (약 6,000줄)

`workspace_runner.py` · `workspace_code_writer.py`의 정적 검사 대부분 · `workspace_coding_handoff.py`의 하네스 생성부 · `interface_contract.py` · `workspace_after_coding.py`

**즉시 삭제하지 않는다.** 모든 대회가 새 모델로 이행을 마친 뒤에 지운다.

---

## 7. 이행 순서

한 번에 갈아엎지 않는다. 새 실행 모델과 기존 모델을 **병행 지원**하고, 대회 단위로 하나씩 옮긴다.

`execution_profile.yaml`에 실행 모델을 선언하는 필드를 둔다:

```yaml
execution_model: contract   # 또는 legacy (미지정 시 legacy)
```

| 단계 | 내용 | 검증 기준 |
|---|---|---|
| **0** | 설계서 확정 (이 문서) | — |
| **1** | `execution_core/` 골격 — 계약 정의, 오케스트레이터, 분할, 제출 작성 | 합성 데이터로 단위 테스트 |
| **2** | 메트릭 소유권 이전 — 대회 온보딩 시 생성, 섭동 검증 | 기존 `runtime_contract` 재사용 |
| **3** | `titanic` 이행 (가장 단순, 표 형식) | **기존 점수와 일치해야 통과** |
| **4** | `bike-sharing-demand` 이행 | 기존 점수와 일치 |
| **5** | `236716` 이행 (per-sample 파일 구조) | 로더 앵커 검증 통과 + 상수 예측 차단 확인 |
| **6** | 구 실행 모델 퇴역 — `legacy` 경로 및 관련 정적 검사 삭제 | 전체 테스트 통과 |

---

## 8. 회귀 방지 규칙

> 이전에 실제로 저지른 실수: 236716을 고치려고 만든 인터페이스 요구사항이 잘 돌아가던
> `bike-sharing-demand`를 차단하게 만들었다. 사용자가 증상을 먼저 발견했다.

**규칙: 실행 모델을 건드리는 모든 변경은, 이미 동작하는 대회 전부에 대해 "차단되지 않는가"를 먼저 확인한다.**

- 3·4단계에서 기존 점수와의 **수치 일치**를 통과 조건으로 둔다. "돌아간다"가 아니라 "같은 답이 나온다".
- 새 검사를 추가할 때는 titanic · bike-sharing-demand · 236716 세 대회 모두에 대해 판정 결과를 먼저 출력해본다.
- 대회 고유 문자열(컬럼명, 파일명, 도메인 용어)이 `execution_core/` 안에 등장하면 설계 오류로 간주한다.

---

## 9. 정직한 한계

1. **상수 예측은 여전히 관찰로만 잡힌다.** 재설계가 이걸 없애지 못한다. 대신 우회 가능한 위치에 놓일 수 없게 만든다.
2. **메트릭 구현은 여전히 생성물이다.** 대회마다 다르므로 피할 수 없다. 소유권을 trial 밖으로 옮겨 *에이전트가 자기 점수를 고칠 수 없게* 만드는 것이 개선의 전부다.
3. **로더는 여전히 대회 구조를 안다.** 그게 로더의 존재 이유다. 위험은 한 파일로 격리되고, 앵커 두 개로 검증된다.
4. **무거운 학습은 직렬화 계층이 필요하다.** 설계에 한 겹이 추가된다.
5. **이행 기간 동안 두 모델이 공존한다.** 6단계를 끝내지 않으면 오히려 복잡도가 늘어난다. 반드시 끝까지 간다.

---

## 10. 다음 작업

- [ ] 1단계: `execution_core/` 골격 — `contract.py`, `orchestrator.py`, `splitting.py`, `submission_writer.py`
- [ ] 2단계: 메트릭 소유권 이전 + 섭동 검증 연결
- [ ] 3단계: titanic 이행 (점수 일치 검증)
- [ ] 4단계: bike-sharing-demand 이행 (점수 일치 검증)
- [ ] 5단계: 236716 이행
- [ ] 6단계: 구 실행 모델 퇴역
